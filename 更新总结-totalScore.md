# 大模型参数更新总结

## ✅ 更新完成

### 📝 更新时间
2026-02-06

### 🎯 更新内容

#### 输入参数更新

**新增必需参数**：`totalScore`（总分）

**完整参数列表（6个）**：
```json
{
  "task": "en-app",           // ✅ 必需
  "model": "llm",             // ✅ 必需
  "content": "学生作文",      // ✅ 必需
  "grade": "高二",            // ✅ 必需
  "totalScore": 15,           // ✅ 必需 ⭐ 新增
  "problem": "作文题目"       // ⚠️ 可选
}
```

#### 输出参数更新

**之前**：
```json
{
  "data": {
    "percentageScore": 72.3,  // 0-100，需要转换
    ...
  }
}
```

**现在**：
```json
{
  "data": {
    "score": 11.3,             // 真实得分（如11.3/15分）
    ...
  }
}
```

---

## 📊 参数对比表

### 输入参数

| 参数 | 更新前 | 更新后 | 说明 |
|------|-------|-------|------|
| task | ✅ 必需 | ✅ 必需 | 无变化 |
| model | ✅ 必需 | ✅ 必需 | 无变化 |
| content | ✅ 必需 | ✅ 必需 | 无变化 |
| grade | ✅ 必需 | ✅ 必需 | 无变化 |
| problem | ⚠️ 可选 | ⚠️ 可选 | 无变化 |
| **totalScore** | ❌ 无 | ✅ **必需** | **新增** |

### 输出参数

| 字段 | 更新前 | 更新后 | 说明 |
|------|-------|-------|------|
| 主字段 | `percentageScore` | `score` | 字段名变化 |
| 取值范围 | 0-100 | 0-满分 | 更符合实际 |
| 后端处理 | ÷100转换 | 直接使用 | **无需转换** ⭐ |

---

## 🎯 核心优势

### 1. 输入更完整
- ✅ 大模型知道满分标准
- ✅ 可以根据不同满分标准评分
- ✅ 15分、25分、60分等不同类型作文

### 2. 输出更直观
- ✅ 真实得分（11.3/15分）
- ✅ 无需后端转换
- ✅ 减少计算误差

### 3. 兼容性保持
- ✅ 仍转换为0-1比例（前端兼容）
- ✅ 字段结构不变
- ✅ 前端无感知

---

## 📄 更新的文档

### 1. 输入规范
📄 **[大模型输入数据结构规范.md](大模型输入数据结构规范.md)**

**主要更新**：
- ✅ JSON Schema添加 `totalScore` 字段
- ✅ 字段说明添加 `totalScore` 详解
- ✅ 所有示例JSON添加 `totalScore`
- ✅ 字段映射表更新

### 2. 输出规范
📄 **[大模型替代AYX-最小改动版.md](大模型替代AYX-最小改动版.md)**

**主要更新**：
- ✅ 输出从 `percentageScore` 改为 `score`
- ✅ 字段说明更新（真实得分，不需要调整）
- ✅ 转换代码更新（仍需转换为0-1比例）
- ✅ 对比表更新

### 3. 快速参考
📄 **[大模型替换AYX-输入简化版.md](大模型替换AYX-输入简化版.md)**

**主要更新**：
- ✅ 核心参数从5个变为6个
- ✅ 参数说明表更新
- ✅ 所有示例添加 `totalScore`

---

## 🔍 实际示例

### 15分应用文

**输入**：
```json
{
  "task": "en-app",
  "model": "llm",
  "content": "Dear Sir, I am writing to apply...",
  "problem": "假定你是李华，写一封申请信",
  "grade": "高二",
  "totalScore": 15
}
```

**输出**：
```json
{
  "data": {
    "score": 11.3,             // 11.3/15分
    "highlight": [...],
    "error_set": [...]
  }
}
```

### 25分读后续写

**输入**：
```json
{
  "task": "en-app",
  "model": "llm",
  "content": "...",
  "problem": "阅读材料，续写两段...",
  "grade": "高二",
  "totalScore": 25
}
```

**输出**：
```json
{
  "data": {
    "score": 18.5,             // 18.5/25分
    "highlight": [...],
    "error_set": [...]
  }
}
```

---

## ⚠️ 重要提示

### 对于N8N工作流

**需要更新N8N工作流**：

1. **输入**：添加接收 `totalScore` 参数
2. **输出**：将 `percentageScore` 改为 `score`（真实得分）
3. **评分逻辑**：根据 `totalScore` 满分输出对应分数

**示例**：
- totalScore = 15 → 输出 0-15 分
- totalScore = 25 → 输出 0-25 分
- totalScore = 60 → 输出 0-60 分

### 对于后端代码

**现有代码仍可用**，因为：
- ✅ `totalScore` 会传递给大模型
- ✅ 输出的 `score` 仍会转换为0-1比例（兼容性）
- ✅ 前端显示逻辑不变

---

## 📋 检查清单

部署前确认：
- [x] 输入规范文档已更新
- [x] 输出规范文档已更新
- [x] 输入参数包含 totalScore
- [x] 输出字段改为 score
- [ ] N8N工作流已更新（需要您操作）
- [ ] 测试通过

---

**版本**: v3.0（包含totalScore）
**更新时间**: 2026-02-06
**状态**: ✅ 文档已更新，等待N8N工作流配置
